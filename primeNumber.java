import java.util.*;

public class primeNumber {
  public static int primeSequence(int range) {
  }

  public static void main(String args[]) {
    var scan = new Scanner(System.in);
    int num = scan.nextInt();
    System.out.println(primeSequence(num));
    scan.close();
  }
}
